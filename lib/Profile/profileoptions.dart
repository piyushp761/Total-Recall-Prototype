import 'package:flutter/widgets.dart';

class ProfileOptions {
  String optionName;
  String optionDescription;
  Icon optionIcon;

  ProfileOptions(
      {this.optionName, this.optionDescription, this.optionIcon});
}